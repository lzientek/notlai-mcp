{
  "name": "notlai-mcp",
  "version": "1.5.0",
  "description": "MCP server for Notlai — Access your notes from Claude Desktop",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "notlai-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "lint": "tsc --noEmit",
    "pack:mcpb": "npm run build:bundle && mcpb pack",
    "build:bundle": "node scripts/build-bundle.js"
  },
  "keywords": [
    "mcp",
    "claude",
    "notes",
    "notlai",
    "model-context-protocol"
  ],
  "author": "Lucas Zientek",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/lzientek/notlai-mcp.git"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.1090.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@anthropic-ai/mcpb": "^2.1.2",
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.6.0"
  }
}
